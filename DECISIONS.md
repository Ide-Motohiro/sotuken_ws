# 決定と実測の記録

卒業研究「LLMを用いたリアルタイム音声対話システムにおける不流暢性と身体性が対話印象に与える影響」。

**片付いた作業の記録。** 決めたこと・測った値・**採らなかった選択肢とその理由**を残してある。
未着手の作業は [TODO.md](TODO.md)、設計上の注意は `CLAUDE.md`、論文向けの記述は `docs/proposed_system_draft.md`。

同じ検討を二度やらないための記録なので、**却下した案の理由を消さないこと。**

---

## 音声出力方式（2026-08-16）

疑似言語音＝合成前クエリの子音置換（`PhonemeSwapTTS`、調音的に近い置換 × 差し替え率0.33）。
聴取比較で選定。試作して却下した4方式とその理由は `CLAUDE.md` の「疑似言語音の実験ツール」にある。

---

## 応答を短く保つシステムプロンプト

**なぜ**：聴取比較で、**発話長は了解性そのものにはほとんど影響しないが、意図を汲み取るときの疲れやすさに影響する**ことが分かった。疑似言語音は解読を要求するので、長い応答ほど負担が積み上がる。了解性は置換の割合で、認知的負担は発話長で制御する、という切り分けになっている。

ところが現在のプロンプトは「あなたは親しみやすいAIアシスタントです。簡潔に日本語で答えてください。」（`main_phoneme_swap.py` の `SYSTEM_PROMPT`）で、**この指示は実質的に効いていない**。上記の実測で57モーラ・再生13.8秒の応答が返ってきている。

**やること**
- [x] 応答を短く保つ指示を設計する
- [x] 実際に何モーラに収まるかを複数の入力で測る
- [x] 読点で区切るかどうかを聞き比べで決める（→ **区切った方が聞きやすい**、2026-08-17に判断）
- [x] 決めた文面を `main_phoneme_swap.py` の `SYSTEM_PROMPT` に反映する（2026-08-17）
- [x] `docs/proposed_system_draft.md` を更新する（X.6.2 に「応答を短く保つためのプロンプト設計」「モーラ数と再生時間の乖離」を追加、X.4 に思考トークン無効化の記述を追加）

### 測定結果（2026-08-16、ユーザー発話5種・各1回、n=5）

| 条件 | モーラ中央値 | 最大 | 秒・中央値 | 秒・最大 |
|---|---|---|---|---|
| 現行（「簡潔に日本語で答えて」） | 80 | 127 | 12.98 | 19.67 |
| 短縮プロンプト（助詞省略あり） | 11 | 19 | 3.82 | 5.50 |
| 短縮プロンプト（文末表現を残す） | 10 | 18 | 2.36 | 4.04 |

「簡潔に」が効かなかった主因は指示の曖昧さというより、**前置き・同調・絵文字・列挙を禁止していなかったこと**。そこを塞ぐだけで80モーラ→11モーラになった。

### 読点あり／なしの比較（`compare_chunking.py`）

同じ内容・同じモーラ数（平均9モーラ）で、読点の有無だけを変えた比較。

| | 平均モーラ | 平均秒 | 秒/モーラ |
|---|---|---|---|
| 読点なし | 9.0 | 2.16 | 0.240 |
| 読点あり | 9.0 | 3.30 | 0.366 |

**聴取の結果、読点ありを採用**（単語単位で聞き取りやすい）。再生時間が1.5倍になるコストは了解性のために受け入れる。

**注意**：モーラ数と再生秒数は連動しない。読点を増やすとモーラ数は変わらないまま秒数だけ延びる。**認知的負担の指標として使うのは秒数の方**であり、モーラ数で代用してはいけない。この乖離自体が論文に書ける観察。

### 確定した文面（`main_phoneme_swap.py` に適用済み）

適用後、例を4つにした最終版で測り直した結果（`thinking_budget=0`、n=8）：

| | 結果 |
|---|---|
| 生成時間 | p50 **614ms**（min 515 / max 4454 / σ 1347） |
| 応答の長さ | p50 **8モーラ**（max 13） |
| 再生時間 | p50 3.00秒（max 4.62秒） |

例を2つから4つに増やしたことで、`thinking_budget=0` の弱点だった内容の不適切さも解消した。「この前の話、覚えてる？」に「へえ、なんの、話、だっけ？」と正しく聞き返し、「疲れてる」にも「あー、たいへん、だね」と噛み合う感動詞を選んでいる（例2つ版では嘘をつき、感動詞も外していた）。

```
あなたは日本語を学習中の、聡明だが言葉が達者でない話し相手です。

[ルール]
1. 返答は1文。長くても2文。
2. 前置き・挨拶・同調（「いい質問ですね」「なるほど」等）は禁止。核心だけを言う。
3. 文頭に感動詞（あー、うん、へえ、おお、うわ、えー）を1つ置く。
4. 読点で単語ごとに区切り、ぶっきらぼうに話す。助詞は減らす。
5. ただし文末は「〜だね」「〜かな」「〜だよ」「〜なの？」を必ず残す。丁寧語は使わない。
6. 説明・理由・列挙はしない。短さを最優先する。

[例]
ユーザー「今日はいい天気だね」→「うん、きもち、いいね」
ユーザー「週末どこか行く？」→「あー、まだ、決めてない、かな」
ユーザー「これ食べていい？」→「だめ、それ、わたしの」
ユーザー「テスト受かったよ」→「おお、やった、ね！」
```

**設計上の要点**
- **「30モーラ以内」のような数値制約は使わない**。LLMはモーラを正確に数えられない。few-shot の例で見せる方が確実に効く
- **助詞を削っても文末表現は必ず残す**。文末は発話の態度（質問・同意・拒否・困惑）を担っており、これは疑似言語音で唯一伝わる情報（`docs/proposed_system_draft.md` X.6.2 の目標水準）。ここを削ると狙いそのものを消すことになる
- 助詞・文末表現は母音と撥音に偏っているため子音置換の影響をほとんど受けない。**壊れずに残る部分**なので温存する価値が高い
- 「鋭い分析を続ける」のような指示は長さの制約と矛盾するため無視される（実測で分析は1つも出なかった）

---

## 合成時間の計測と高速化

**なぜ**：この研究の主題は体感リアルタイム性で、フィラーで隠すべき区間は「ユーザーの発話終了 → 音が鳴り始めるまで」。ところが現在の `VoiceVoxTTS.speak()` は `winsound.PlaySound()` が再生完了までブロックするため、合成にかかった時間と再生時間が合算されてしまい、**隠すべき区間そのものが測れていない**。

実測例（2026-08-16、`--swap-ratio 0.5`、マイクを使わない後半のみの通し確認）：

| 応答 | Gemini | 合成＋再生 |
|---|---|---|
| 「こんにちは！本当に気持ちの良いお天気ですね。」 | 2.41秒 | 5.52秒 |
| 「私はAIなので、どこかへ出かけることは…（57モーラ）」 | 1.81秒 | 13.82秒 |

Gemini の所要時間だけは分離できていたが、合成が何秒かは不明だった。

### `speak()` の分割と区間計測

- [x] `VoiceVoxTTS` を `synthesize()` / `play()` / `speak()` に分割する
- [x] 3区間（audio_query / synthesis / 再生）を個別に計測する
- [x] 計測結果を取り出せるようにする（`tts.last_timing` と `on_timing` コールバックの2通り）
- [x] `measure_voicevox_latency.py` との重複を確認する（→ `timed_audio_query()` / `timed_synthesis()` が既にあった。あちらはオフライン計測、今回は本番経路の中での計測なので役割が違う。共通化はしていない）

`SynthesisTiming`（`infrastructure/voicevox.py`）が3区間を持ち、`time_to_first_sound_sec` = query + synthesis（**フィラーで隠すべき区間**、再生は含まない）を提供する。

`synthesize()` と `play()` は `is_speaking` を触らない契約にしてある（`speak()` が全体を通して管理する）。項目4でフィラーの停止点を「再生の直前」に置くとき、この隙間を使う。

### 測定結果（2026-08-17、`PhonemeSwapTTS`、ウォームアップ後）

| 応答 | audio_query | synthesis | 音が鳴るまで |
|---|---|---|---|
| 「うん、いい、天気、だね」 | 6ms | 1272ms | **1278ms** |
| 「あー、まだ、決めてない、かな」 | 6ms | 1073ms | **1079ms** |
| 長い応答（35モーラ） | 20ms | 1623ms | **1643ms** |

**判明したこと**

- **合成に1〜1.6秒かかる。** 想定より大きい
- **応答を短くしても合成は1秒を切らない**（最短1073ms）。プロンプト側の改善ではこの区間は縮まない
- `audio_query` は6〜20msでほぼ無視できる。子音置換も同じ経路に入っているので、**置換のコストは実質ゼロ**と確認できた

### 高速化の調査（2026-08-17）— 効いたのは thinking の無効化だけ

同じ道を二度歩かないよう、効かなかったものも理由付きで残す。

| 手段 | 結果 |
|---|---|
| `outputSamplingRate` を下げる | **効かない**。24000Hz 1135ms / 8000Hz 1167ms。ニューラル推論が支配的で出力解像度は無関係 |
| 文単位で分割合成 | **使えない**。長文なら有効（一括2889ms → 最初の1文1718ms）だが、応答を短くする方針を採った結果ほぼ1文になり、分割の余地が無い |
| VOICEVOX を GPU（DirectML）に | **遅くなった**。下表参照 |
| ポーズ分の合成を省く | 効くが見送り。下記「無音のコスト」参照 |
| **Gemini の thinking を無効化** | **5.7倍速くなった**。採用 |

#### VOICEVOX の GPU / CPU（各15回）

| | CPU | GPU（DirectML） |
|---|---|---|
| 短い応答（11モーラ） | p50 **1104ms**、幅1032〜1132、**σ=33** | p50 971ms、幅902〜**1839**、σ=367 |
| 中くらい（35モーラ） | p50 **1639ms**、幅1628〜1685、**σ=18** | p50 **2163ms**、幅1328〜2196、σ=216 |

短い応答では GPU が中央値130ms速いが最悪値が1839ms、中くらいでは GPU が中央値520ms遅い。**CPU はばらつきがほぼ無い**（15回で最大最小の差が100ms以下）ため、合成時間が事前に予測できる。レイテンシが従属変数である以上、予測可能性の方が価値が高いと判断して **CPU のまま**とする。AMD 内蔵GPU + DirectML ではこの規模のモデルはオーバーヘッドが利得を上回っていると推測（内部は未確認）。

#### 無音のコスト

合成時間は**出力音声の秒数に比例し、無音も発話と同じ単価**でかかる。`pauseLengthScale` だけを変えた実測（他は完全固定）：

| pauseLengthScale | 出力音声 | 合成時間 |
|---|---|---|
| 0.0 | 2.24秒 | 746ms |
| 1.0（既定） | 3.48秒 | 1118ms |
| 4.0 | 7.21秒 | 2093ms |

**合成時間 ≒ 140ms + 271ms × 出力音声の秒数**。読点ありの応答では音声3.48秒のうち1.24秒（36%）が無音で、その生成に372msかかっている。

→ `pause_mora` の長さを0にして合成し、出来た wav に無音を挿し戻せばその分だけ速くなる。

#### 検証（2026-08-19、`compare_pause_splice.py`）

実際に応答5種で測り、試作まで作った。

| 応答 | 無音の割合 | 素の合成 | 無音なし | 短縮 |
|---|---|---|---|---|
| うん、きもち、いいね | 35% | 879ms | 668ms | 211ms |
| あー、まだ、決めてない、かな | 36% | 1171ms | 817ms | 354ms |
| うん、ロック、よく、聴く、かな。 | 42% | 1284ms | 844ms | 441ms |
| おお、最近、シティポップ、良い、だよ。 | 39% | 1499ms | 1016ms | 484ms |
| うん、きみに、あう、もの、探す、よ。 | 44% | 1573ms | 974ms | 599ms |

**中央値で441ms（合成の34%）短縮できる**。無音区間全体（約2.1秒）の約21%。

**クリックノイズの懸念は実測で消えた。** 無音を長さ0にしても VOICEVOX は句末の母音を無音まで減衰させるため、挿入点の振幅は0〜3（最大振幅32767に対して）だった。波形が不連続にならないので、無音を挟んでも段差が生じない。

挿入位置はクエリの長さ合計から計算する。予測は最大15msずれるので、予測位置の±25msで振幅が最小の点に吸着させてから挿す。挿し戻した音声の長さは素の合成と±15ms以内で一致する。

#### 却下（2026-08-19、聴取による）

**ぶつ切りに聞こえる。** 波形上の指標では問題が出ないのに、耳では明確に劣化した。

原因は特定できた。**素の合成の「無音区間」は無音ではない。** 発話に対して約-30dB のノイズフロア（無音区間の RMS 23〜49、発話全体は約1000）が常に乗っていて、挿し戻しでゼロを挿すとそれが読点のたびに400ms前後消える。音が途切れたように聞こえるのはこのため。

ノイズフロアを埋め戻す版も作った（発話冒頭の `prePhonemeLength` の区間を素材にして往復させて並べる）。最も静かな200msの RMS はゼロ埋めの 0.0 から 19〜43 まで戻り、素の 16〜28 に近づいた。**それでも聴取では改善が足りず、却下**。

| | A（素） | C（ゼロ埋め） | D（ノイズ埋め） |
|---|---|---|---|
| うん、きもち、いいね | 28.0 | 0.0 | 25.6 |
| あー、まだ、決めてない、かな | 23.5 | 0.0 | 19.3 |
| おお、最近、シティポップ、良い、だよ。 | 16.2 | 0.0 | 43.1 |
| うん、きみに、あう、もの、探す、よ。 | 15.6 | 0.0 | 38.2 |

**却下してよい理由**：短縮できるのは合成の441msで、**この区間はフィラーが埋めている**。音質を犠牲にして得るものが小さい。「音質は落とさない」という方針は、波形加工（`compare_blur.py`）を却下したときと同じ基準で適用する。

**再検討する場合の材料**：
- 検証スクリプトは `compare_pause_splice.py` に残してある（`--with-zero` / `--with-bare` で各版を鳴らせる）
- ノイズフロアの音量が素と合っていない（上表の3番・4番は素の2.5倍＝+8dB）。素材をその発話の冒頭から採っているため、冒頭が賑やかな発話ではポーズも賑やかになる。ここを合わせても直るかは未検証
- 無音を抜いて合成すると VOICEVOX が見る前後の文脈が変わるので、句末の母音の減衰そのものが変わっている可能性がある。これはノイズフロアの調整では直らない

#### 波形上は問題が出なかった（指標の限界の記録）

挿入点の振幅は0〜3（最大振幅32767に対して）、挿し戻した音声の長さは素と±15ms以内で一致していた。**この2つが揃っていても聴取では落ちた。** 「クリックノイズが出ないこと」を波形の不連続だけで判定したのが誤りで、ノイズフロアの消失という別種の劣化を見落としていた。音質の判断は耳で行うこと。

#### Gemini の thinking（実装済み）

**制約の多いプロンプトほど思考が長引く。** 応答を短くする指示を足したことが、逆に生成を遅くしていた。

| プロンプト | thinking | p50 | 幅 |
|---|---|---|---|
| 旧（長い応答） | 既定 | 1967ms | 893〜6034 |
| 短縮（12文字の応答） | 既定 | **3298ms** | 1494〜7401 |
| 短縮 | **0** | **579ms** | 523〜3852 |

出力が5分の1なのに生成は2倍近く遅い、という逆転が起きていた。

**出力の質**：文体の制約はどちらも同等に守られる。落ちるのは内容の適切さで、`thinking=0` では「この前の話、覚えてる？」に対し文脈が無いのに「うん、おぼえてる、よ」と答えた（既定は「あー、なんの、話、なの？」と正しく聞き返した）。感動詞の選択も噛み合わないことがある（「疲れてる」に「おお」）。

**それでも0を採用する理由**：疑似言語音では語彙が聞き手に届かないため内容の精度が効きにくく、レイテンシは従属変数そのもの。ただし**態度は届く**（唯一の設計目標）ので、上の「覚えてる」のような態度の誤りは無視できない。

- [x] `GeminiLanguageModel` に `thinking_budget` を追加（`None`=API既定、`0`=無効化）
- [x] `main_phoneme_swap.py` の既定を0にする（`--thinking-budget` で変更可、`-1` でAPI既定に戻る）
`main.py`（素の音声合成）は内容がそのまま届くため既定のままにすること。

#### 中間値の測定（2026-08-19、3回 × 5ケース、確定した `SYSTEM_PROMPT` を使用）

「思考を少しだけ許せば、速さを保ったまま内容の精度が戻るのではないか」を確かめた。**戻らなかった。**

| budget | 生成 p50 | 生成 最大 | 思考トークン中央 | 応答のモーラ中央 | 本物のルール違反 |
|---|---|---|---|---|---|
| **0（採用）** | **0.60秒** | 1.85秒 | 0 | 8 | 0 |
| 128 | 1.17秒 | 2.48秒 | 93 | 8 | 0 |
| 512 | 2.63秒 | 3.84秒 | 340 | 8 | 0 |
| 既定 | 2.11秒 | **17.75秒** | 209 | 8 | 1 |

- **応答の長さも文体の遵守も budget で変わらない。** モーラ中央値は全条件で8。ルール7（聞き返しへの言い直し）も `0` で守れている（「え？なんて？」→「てんき、いいね、って」→「よてい」と段階的に短くなる）
- 唯一の本物の違反は既定条件の1件（聞き返しへの応答に感動詞を付けた）。思考を許すほど遵守が良くなる、という関係は見えない
- **既定条件で 17.75秒 の外れ値が出た。**「最近ちょっと疲れててさ」への応答で思考2351トークン。共感を要する発話ほど思考が伸びる。実験中に出れば1試行が壊れる
- レイテンシは budget にほぼ比例して悪化する。**払う対価に対して得るものが無い**

→ **中間値は不採用。`0` で確定**。この項目は閉じた。

### 無音区間の内訳（確定、2026-08-17）

| 区間 | thinking 既定 | **thinking 0（採用）** |
|---|---|---|
| 終端判定の待ち | 0.4秒 | 0.4秒 |
| Gemini | 3.3秒（71%） | 0.58秒（28%） |
| 合成 | 1.1秒 | 1.1秒（**52%**） |
| **合計** | **4.8秒** | **2.1秒** |

**最大の要素が Gemini から合成に移った。**

**ただしこの表はオフライン測定の足し算であり、実対話の値ではない**（下の「通しレイテンシの実測」参照）。
実対話では約2倍かかる。この表は区間ごとの相対的な大きさを見るためのものとして扱うこと。

### 通しレイテンシの実測（2026-08-20、実対話7ターン）

`TurnTiming` を本番経路に入れて測った。**オフライン測定の足し算とは倍近く違う。**

| 区間 | オフライン測定 | **実対話（中央値）** | 最小〜最大 |
|---|---|---|---|
| 終端判定 | 0.4秒 | **0.97秒** | 0.42〜1.12 |
| 応答生成 | 0.51秒 | **1.33秒** | 0.68〜2.29 |
| 合成〜最初の音 | 0.71秒 | **1.10秒** | 0.63〜1.73 |
| フィラーの待たせ | — | 0.00秒 | 0.00〜0.16 |
| **発話終端から応答の音まで** | 1.62秒 | **3.21秒** | 2.19〜5.26 |
| （参考）応答の再生 | 1.88秒 | 3.86秒 | 1.76〜5.79 |

**ズレた理由**：

- **生成が2.6倍**。オフライン測定は毎ターン履歴を空にして1ターンだけ投げていたため、
  履歴が積み上がる実対話の条件を再現できていなかった
- **合成が長いのは応答が長いから**。再生時間の中央値が 1.88 → 3.86秒。
  ルール1（1文、長くて2文）は守られているが、1文が長い
- **終端判定が0.97秒**。理由は下の「終端はほぼ Google 側が確定させている」

**この遅さは許容と判断した（2026-08-20）。** 実際に対話して「そんなに不自然ではない、
ゆるりとした感じで良い」との評価。**体感が成立している以上、レイテンシ短縮は優先度が低い。**
今後この数字を見て高速化に手を出す前に、まず体感が壊れているかを確かめること。

### 終端はほぼ Google 側が確定させている（2026-08-20、前提の訂正）

**7ターン中6ターンが `is_final` で確定した。** それまで「interim が0.4秒変化しない経路が主に効く」と
書いていたが**逆だった**。

原因はコードを読み直して分かった。**interim の無変化チェックは Google から結果が届いたときにしか
評価されない**（`for result in response.results:` の中にある）。ユーザーが黙っている間に
Google が中間結果を送ってこなければ、0.4秒経っても誰も判定しない。先に `is_final`（約1.0秒）が来る。

**影響**：継続語尾による待ち時間の切り替え（「やっぱ」対策）は **interim 経路でしか効かない**。
7ターン中1ターンでしか働いていない。「本読むのってやっぱ」の打ち切りがどちらの経路だったかは
計測を入れる前なので不明で、**あの修正が効いたという確証は無い**。

### 認識結果の信頼度は取れる（2026-08-20、前提の訂正）

「Google STT の `confidence` は最終結果にしか入らず、本システムは interim 経路が主なので
取れないだろう」と書いていたが、**上の訂正により前提が崩れ、実際には取れていた**。

| ターン | 経路 | 信頼度 | 認識結果 |
|---|---|---|---|
| 1 | `is_final` | **0.72** | テイストです（「テストです」の誤認識） |
| 2〜5, 7 | `is_final` | 0.87〜0.98 | 正しく認識 |
| 6 | interim | 0.00 | （interim には入らない） |

**誤認識したターンだけ信頼度が明確に低い。** 1例なので断定はできないが、閾値で弾く価値はありそう。

---


---

## 聞き返し・誤認識へのロバストネス

**なぜ**：疑似言語音では相手に言葉が届かないため、対話中に「え？なんて？」が必ず起きる。実験セッションが成立するかに直結する。短縮プロンプトの実測（2026-08-16、履歴を引き継いだ6ターン）で問題が2つ出た。

| 入力 | 応答 | 長さ |
|---|---|---|
| こんにちは、今日はいい天気だね | うん、いい、天気、だね | 9モーラ |
| **え？なんて？** | えー、いい、天気、言った、の | 11モーラ |
| ごめん、もう一回言って | あー、いい、天気、だね | 9モーラ |
| うん、きもつぃ、いいね？ | うん、きもち、だね | 7モーラ |
| **とんにつぃわ ぱんとお もい** | おお、パン、**牛乳**、だね | 10モーラ |

なお、聞き返されても応答が長くならないことは確認できた（長さの制約が勝っている）。心配していた「すみません、〜と申し上げました」への膨張は起きない。

### 問題1：聞き返しても同じ音が返る

置換表が固定なので、同じ内容を言えば必ず同じ音になる。人間なら言い換える・ゆっくり言うところを、LLMは自分の声が届いていないことを知らないため同じ内容を返し、**同じ雑音がもう一度鳴るだけ**になる。聞き返しが構造的に無意味。

- [x] **対処済み（2026-08-17）**：`SYSTEM_PROMPT` にルール7と `[聞き返しの例]` を追加した。

**求めるのは「言い換え」ではなく「言い直し」**。最初は「別の言葉で言い直す」と書いたが、それだと話題ごと変わってしまい、人間の振る舞いから外れた。

```
（言い換え・却下）              （言い直し・採用）
え？なんて？ → 天気、よい、のだな   え？なんて？ → てんき、いいね、って
え？         → 空、きれい、だよ     え？         → てんき
```

前者は「空、きれい」と別のことを言い出している。後者は話題を保ったまま段階的に削れ、最後は単語1つになる。人が聞き返されたときの縮退そのもの。3つの話題で試して同じ振る舞いを再現した。

**効いたのはルールの文章より `[聞き返しの例]` の追加**。ルール7の文章自体は長くなったのに、例を足したことで他のルールの遵守はむしろ改善した（言い換え版で出ていた「いい質問だね」というルール2違反が消えた）。

**副作用**：生成が p50 614ms → 897ms（1.46倍）に伸びた。制約を足すと生成が遅くなる傾向はここでも出ている。無音区間は約2.1秒 → 約2.4秒。フィラー単発（0.82秒）の被覆率は49%→34%に下がるが、項目4の「単発＋沈黙」の形は成立する範囲。

**やらないと決めたこと**：語彙の自然さ（「つかれている」対「つかれてる」等の縮約形）は調整しない。疑似言語音では聞き手に届かず、ルールを足すぶん生成が遅くなり他のルールの遵守も緩むため。教科書的な言い回しは「日本語を学習中」という設定とも整合する。**参加者にテキストを見せる場合のみ再考が要るが、字幕は使わない方針なので該当しない。**

### 問題2：ASRが疑似言語音や雑音を拾うとLLMが意味をでっち上げる

「とんにつぃわ ぱんとお もい」に対して「おお、パン、牛乳、だね」と返した。**牛乳は入力のどこにも無い**。崩れた入力から勝手に意味を作っている。

エコー防止（`is_speaking`）があるため再生中の回り込みは防げるが、マイクの回り込みや参加者の復唱で類似の入力は起こりうる。しかも**出力が自然に見えるため、起きたことに気付けない**。

- [x] **対処済み（2026-08-17）**：`DialogueApplicationService` に `on_turn` コールバックを追加し、1ターンごとに（認識テキスト, 応答テキスト）を受け取れるようにした。`main_phoneme_swap.py --log PATH` で JSONL に追記する（合成時間も併せて記録）。**実験時は必ず `--log` を指定すること。**

```json
{"time": "...", "user": "...", "reply": "...", "query_sec": 0.006, "synthesis_sec": 1.104, "playback_sec": 3.01}
```

---

## 通し動作の確認（2026-08-19、21ターン）

マイク入力を含む end-to-end が動くことを確認した。`.env` の `STT_INPUT_DEVICE=USB PnP` で
入力デバイスを固定している（OS既定は「ステレオ ミキサー」で、そのままでは声が入らない）。

| 区間 | 中央値 | 最小 | 最大 |
|---|---|---|---|
| `audio_query` | 0.007秒 | 0.004 | 0.015 |
| 合成 | 0.771秒 | 0.407 | 1.244 |
| 再生 | 2.645秒 | 1.053 | 4.548 |
| **フィラーが応答を待たせた時間** | **0.000秒** | 0.000 | 0.356 |

- 合成0.771秒はオフライン測定（711ms）とよく一致する
- **フィラーの待たせは中央値0秒**。待たせてよいと決めたが、実際にはほとんど発生していない。
  最大0.356秒は見積もり（0.28秒）とほぼ一致
- **応答の文頭に感動詞が出た割合は 4/21（19%）**（すべて「うん」、すべて同意の場面）。
  ルール6が効ききっていないが、**この頻度は許容と判断した**。
  下げたい場合は [例] 側に同意の場面を感動詞なしで足すのが有効（few-shot の方がルール文より効く）

---

## 発話終端の判定

**なぜ**：中間結果が `stability_duration`（0.4秒）変化しなければ確定、という簡易VADで終端を取っている。
これはフィラーが鳴り始める**前**の区間なので、**伸ばすと隠せない無音がそのまま増える**。

### 続きそうな終わり方では長く待つ（2026-08-19）

**発話の途中の短い休止で確定してしまう**、と実対話で判明した。「昨日は……映画を見た」の
「は」のあとで切られる。一方でテンポは良いと評価されていたので、0.4秒を一律に伸ばす案は採らない。

日本語の発話中の休止は**文節末（助詞・接続助詞の直後）に集中する**ので、そこだけ長く待つ。

- `domain/models.py` の `looks_incomplete()` が、中間結果の**終わり方だけ**を見て判定する
- 真なら `continuation_duration`（既定1.0秒）、偽なら `stability_duration`（既定0.4秒）
- `--endpoint-wait` / `--endpoint-wait-continuing` で変更できる

**意味を解釈しない。** 認識の中間結果は句読点を持たないので文の完結は語尾でしか見分けられず、
解釈を挟むと条件間で挙動がぶれる。判定を誤って長い方に倒れても損は待ち時間だけで、
発話が壊れることはない。だから迷ったら入れる方針で語を選んである。

対象：接続助詞（て・で・けど・が・し・から・ので・のに・たら・なら・ば・とか・って）、
格助詞と係助詞（は・も・を・に・へ・と・の・や）、言いよどみ（えーと・あの・その・まあ・なんか）、
述語を要求する副詞（やっぱ・やっぱり・たぶん・きっと・すごく・めっちゃ・あんまり・かなり・けっこう・たしか・なんとなく）、
連体詞（こんな・そんな・あんな・どんな）。

副詞・連体詞は**実対話の失敗から足した**。「本読むのってやっぱ……おもしろいよな」が
「本読むのってやっぱ」で確定し、Gemini は「うん、いい、よね。」と**破綻しない応答を返した**。
認識が壊れていても出力は自然に見えるので、ログが無ければ気付けない（上の「問題2」の実例）。

`もう` は入れていない。`endswith` で見ているため意志形（「本読もう」）を巻き込む。

**既知の穴**：

- **`ね` `さ` は入れていない。** 「昨日ね、」の間投助詞と「いい天気だね」の終助詞が同じ形で、
  入れると普通の文末でも毎ターン1.0秒待つことになる。テンポへの影響が大きすぎる
- **助詞を伴わない休止は拾えない。** 「えーと、ラーメン……が好き」の「ラーメン」のあとなど。
  語尾に手がかりが無いので、この方式では原理的に検出できない。残るなら `--endpoint-wait` を上げる
- 偽陽性はある（「これ、わたしの」の `の`、「ちょっと」の `と`）。costs は待ち時間だけ

---

## フィラー挿入機構

**これが実験の独立変数そのもの**（sub-RQ1）。方針は**静的で単純**。生成タイミングや内容を動的に変える仕組みは作らない。フィラーは実験の操作対象なので、凝った仕組みほど「フィラーの有無」以外の差を持ち込む。

### 実装したもの

- `domain/interfaces.py` の `FillerPlayer`（start / stop、stop は冪等）
- `infrastructure/filler.py` の `VoiceVoxFillerPlayer`
- `VoiceVoxTTS.on_before_playback` フック（**合成後・再生前**に呼ばれる）
- `DialogueApplicationService(filler=...)` — 応答生成の前に開始、`finally` で保険の停止
- `main_phoneme_swap.py --no-filler` / `--filler-interval`
- `google_stt/play_filler.py` — フィラーを実際に鳴らして確かめるツール（本番と同じクラスを使う）

- [x] フィラー再生を `run_once()` 側へ移す
- [x] 起動時に事前合成する（5パターンで約2秒。鳴らす直前に合成すると隠したい遅延を自分で作る）
- [x] 選択方式を決める（→ **直前と同じものは選ばない無作為選択**。下記）
- [x] フィラーの種類を決める（5パターン。下記）
- [x] 1回鳴らして止める。ループしない
- [x] 外れ値への対処（固定間隔で2つ目）
- [x] 有無を実行時に切り替える（`--no-filler`）
- [x] フィラー再生中も `is_speaking` を立てる
- [x] 停止の遅延を測る（`last_stop_delay_sec`）

### 選択方式（2026-08-17）

当初は決定的なローテーションにしていた（理由：「乱数だと再現性が落ちる」）。**この理由は誤りだったので無作為選択に変更した。**

守れているつもりだった再現性は実際には存在しない。参加者ごとにターン数も発話内容も違うので、決定的に回しても人によって聞くフィラーの並びは揃わない。一方で決定的だと**周期に気付かれる**リスクがある（5種なら5ターンで一巡）。機械的に感じられると親しみやすさに逆行する。

現在は**直前と同じものは選ばない無作為選択**（選択状態はターンをまたいで保持）。`seed` を渡せるようにしてあるが、これは参加者間の統制のためではなく**バグ追跡のため**。既定では実行ごとに並びが変わる。

### フィラーの種類（2026-08-17）

| # | フィラー | 長さ |
|---|---|---|
| 1 | えーと | 811ms |
| 2 | あのー | 768ms |
| 3 | うーん | 789ms |
| 4 | えーっと | 917ms |
| 5 | あー | 683ms |

「えーと」と「えーっと」は促音の有無だけで実質同じ音に聞こえる。無作為選択に変えたので並び順自体に意味は無いが、種類を減らすならこの2つのどちらかが候補。

種類を変えるなら `play_filler.py --phrases` で試してから `DEFAULT_FILLER_PHRASES` を書き換える。

### 停止の位置

**合成の後・再生の前**。応答テキストが返った時点で止めると、そのあとの音声合成 約1.1秒がまるごと無音になる。`on_before_playback` に `stop` を配線して実現している。`run_once()` の `finally` にもある停止は、配線漏れ・例外時の保険。

テストで `start → synthesize → stop → play` の順序を固定してある（`domain/test_application.py`）。

### 応答の文頭から感動詞を外した（2026-08-19）

**フィラーと応答冒頭の感動詞が二重になり、しかも人とは逆順だった。**

```
それまで：  えーと（言い淀み） → うん、きもち、いいね（反応）
人の順番：  うん（反応）       → えーと、きもち、いいね（言い淀み）
```

語の集合も重なっていた（フィラーの `あー` と旧ルール3の `あー`、`うーん` と `うん`）。
「あー」…（沈黙）…「あー、まだ、決めてない、かな」が実際に起こりうる状態だった。
対話して食い気味・不自然と判断し、旧ルール3（文頭に感動詞を1つ置く）を削除、
代わりにルール6（文頭に感動詞を置かない）を入れた。[例] からも感動詞を外した。

**懸念していた「態度の手がかりが減る」は成立しなかった。** 子音置換で壊れないのは
子音を持たないモーラだけで、旧ルール3の語のうち確実に残るのは `うん` `あー` `おお` `えー` のみ
（`へえ` は `フェエ`、`うわ` は `ウラ` になる）。フィラー自体が子音を持たない感動詞なので、
態度の手がかりはフィラー側が担える。

**「文末は子音置換で壊れない」は誤り**（それまで CLAUDE.md にそう書いていた）。差し替えは
モーラの並び順で機械的に決まるので、`かな` は `タナ`、`なの？` は `ナモ` になる。
残るのは疑問の抑揚（ピッチ）であって語ではない。

#### 測定（4回 × 5ケース、thinking=0）

| | 旧（感動詞あり） | 新（感動詞なし） |
|---|---|---|
| Gemini 生成 p50 | 0.60秒 | **0.51秒** |
| 応答のモーラ中央値 | 8 | **6** |
| 再生時間 中央値 | 2.85秒 | **1.88秒** |
| 合成時間 中央値 | 888ms | **711ms** |
| ルール違反（20件） | — | **0** |

聞き返し（ルール7）は全4回とも正しく言い直した。文頭の感動詞は20件すべてで出なかった。

**無音区間が 約2.1秒 → 約1.62秒 に縮んだ**（0.4 終端判定 ＋ 0.51 Gemini ＋ 0.71 合成）。
却下した「無音を抜く案」の441msより大きい短縮が副産物で得られたことになる。

#### 副作用：フィラーの遅延予算が消えた

| | 旧 | 新 |
|---|---|---|
| 最短の待機区間 | 1.30秒 | **1.04秒**（Gemini 0.40 ＋ 合成 0.64） |
| フィラー最長（えーっと 0.92秒） | 0.92秒 | 0.92秒 |
| **遅延に使える上限** | 約0.38秒 | **約0.12秒** |

立ち上がり0.4秒のままだと、速いターンで**フィラーを鳴らし終わるまで応答が最大0.28秒待たされる**。

→ **待たせてよい、と決めた（2026-08-19）。** 応答を最速で届けることに価値を置かない、という判断。
立ち上がり0.4秒は耳で決めた値なので、そちらを守って応答の側を譲る。
副次的な利点として、フィラーと応答が途切れずに繋がるので音の並びはむしろ自然になる。
`えーっと` を外す案・立ち上がりを0.2秒に縮める案は、どちらも不要になったので採らない。

**交絡は残るので隠さず測る。** フィラーあり条件だけ応答が遅くなるのは事実で、
`main_phoneme_swap.py --log` がターンごとに `filler_stop_delay_sec`（実際に待たされた秒数）を
記録する。論文で条件間のレイテンシ差を述べるときはこの実測値を使うこと。

### 差し替え率は0.33（2026-08-27）

推移：**0.5 →（0.67 を29ターン試して撤回）→ 0.5 → 0.33**。

#### 0.33まで下げた理由：開発者の耳では了解性を判定できない

**初めて聞く人には聞き取れなかった。** これが決定的な指摘で、それまでの値をすべて無効にする。

開発者は**応答の中身も、システムプロンプトの文体も、置換表そのものも知っている**。
「トコモ」を聞いて「ここの」に戻せるのは、戻せる情報を持っているから。
実験参加者は何も知らない状態で聞く。**つまり 0.5 も 0.67 も、了解性を過大評価した値だった。**

**今後、差し替え率の判定を自分の耳で行わないこと。** `compare_intelligibility_grid.py` を
自分で聞いて決めた 0.5 も、実対話で決めた 0.67 も、同じ偏りを持っている。
値を動かすときは、システムを知らない人に聞いてもらうこと。

なお 0.67 を撤回したとき「短時間の聞き比べと継続使用で評価が一致しない」と書いたが、
**本当の原因はこちら（聴取者が情報を持っていること）だった可能性が高い**。

#### 0.67 を試して戻した経緯（2026-08-20〜27）

一度0.67を既定にして実対話で29ターン（3日）使い、0.5に戻した。
`logs/0825-01.jsonl` `0826-01.jsonl` `0827-01.jsonl` がその記録。
以下は0.67を選んだときの記録で、判断の材料として残す。

#### 0.67を選んだときの経緯（2026-08-20、後に撤回）

**0.5では分かりやすすぎた。** オウム返しを止めて応答に新情報が入ったあとで聞き直した評価は
「0.5は結構わかりやすい。0.67だと『ん？』ってときがあって少し面白い」。
狙いは「わかりそうでわからない」なので、**その『ん？』が目標そのもの**。既定を0.67に変更した。

**私の予想は外れた。** オウム返しを止める前に「新しい情報を返すようになると了解性の要求が上がる
（＝差し替え率を下げたくなる）」と書いたが、逆だった。理由は考えれば分かる：
**オウム返しの間は応答に情報が無いので、了解性を試す対象がそもそも無かった。**
「わかりそうでわからない」は復元すべき中身があって初めて成立する緊張で、
0.5で成立して見えていたのは中身が無かったからにすぎない。

**態度の手がかりは0.67でも失われない。** 実際の応答で0.5と0.67を比べたところ、文末の壊れ方は
ほぼ同程度で、どちらも語としては壊れる。残るのは**疑問の抑揚（ピッチ）**の方で、
そこは差し替え率に影響されない。唯一の設計目標（態度が伝わること）は保たれる。

| 応答 | 0.5 | 0.67 |
|---|---|---|
| うみ、いきたい、かな | ウ**ニ**、イキ**パ**イ、カ**マ** | ウ**ニ**、イキ**パ**イ、**タ**ナ |
| どんな、ほん、よむの？ | **ボ**ンナ、**フォ**ン、ヨ**ヌ**ノ | **ボ**ンナ、**フォ**ン、**リョ**ム**モ** |

**当時の判断**：0.5 を選んだ以前の聴取比較（`compare_intelligibility_grid.py`）を否定するものではない。
あちらは固定の文章を単体で聞く条件で、対話の文脈も新情報も無い。条件が違えば最適値も動く、
というだけのこと。（比較ツールの既定も 0.67 に合わせたが、撤回に伴い 0.5 へ戻した。）

### 「覚えているふり」を禁止した（2026-08-27）

**会話履歴は保存されていない。** `DialogueHistory` は `main()` で毎回新しく作られる
メモリ上のオブジェクトで、読み書きするファイルは無い（入出力はログの追記のみ）。
Vertex AI の `generate_content` も毎回 contents を全部送る方式で、サーバー側に状態は残らない。

**にもかかわらず「前の会話を覚えている」ように見えた。原因はモデルの作り話。**
空の履歴に対して以前のやり取りを前提とする発話を投げると、話を合わせて内容をでっち上げる。

| 発話（履歴は空） | 旧 | 新（ルール8） |
|---|---|---|
| この前の本の話どうなった？ | ほん、まだ、よんでない、かも | なんの、ほん、のこと、かな？ |
| 前に言ってたやつどう思う？ | おもしろかった、かな。 | なんの話、してたの？ |
| 先週のあの店よかったよね | うん、**おさけ**、おいしかったね | どの、みせ、だったかな |

**6件中3件 → 0件。** 通常会話への影響は見られず、生成時間も伸びなかった。

**ルール7が悪化させていた可能性がある。**「相手の発話に無い言葉を必ず入れて話を前に進める」は、
前提を欠いた発話に対しては「無い情報を作る」方向に押す。ルール8はその例外を明示したことになる。

**確認用に `--log` へ `history_turns`（そのターン時点の履歴の発話数）を出すようにした。**
起動直後の1ターン目は必ず 2 になる（このターンのユーザー発話＋応答）。
2 以外なら履歴が持ち越されていることになるので、疑ったときはここを見る。

### 読点を分節単位に粗くした（2026-08-27）

**単語ごとの区切りが細かすぎた。**「ねむい、とき、なに、する、かな」を
「ねむいとき、なにするかな」くらいにしたい、という要望。

**外すだけでは失敗する。**「読点で単語ごとに区切り」は**短さの制約も兼ねていた**。
これを「意味のまとまりごとに」に置き換えただけの版（v1）では、応答が普通の日本語に戻った
（「サイエンスフィクション、興味あるかな。」）。

採用したのは、**「助詞を落とす」を独立させ、[例] も片言のまま分節区切りに書き換えた版**（v2）。

| | 現行（単語ごと） | v1（読点だけ緩めた） | **v2（採用）** |
|---|---|---|---|
| 読点 中央 | 2 | 1 | **1** |
| モーラ 中央 | 8 | 13 | 11 |
| **再生 中央** | 2.53秒 | 2.54秒 | **2.40秒** |
| **合成 中央** | 863ms | 837ms | **810ms** |
| 生成 p50 | 0.66秒 | 0.76秒 | 0.73秒 |

出力例：「ねむいとき、なにするかな」「おきられない、こまるね」「どんな本、よんだの？」。
聞き返しも保たれている（→「ねむいとき、って」→「ねむい」）。

**モーラ数は増えたのに再生時間は減っている。** 読点を減らすと無音が消えるためで、
[モーラ数と再生秒数は互換でない](#) という既知の性質がここでも効いている。
認知負担の指標は再生秒数の方なので、**v2は増えたのではなく減っている**と読む。

### オウム返しを禁止した（2026-08-20）

**実対話で話が前に進まなかった。** ログから：

| ユーザー | 応答（旧） |
|---|---|
| なんかお昼寝がしたい気分 | 昼寝、したい、気分だね。 |
| 1時間とかで起きることができない | 1時間、起きる、難しい、だね。 |
| だから昼寝できないんだよね悲しい | 昼寝、できない、悲しい、ね。 |

原因は短さの制約そのもの。「説明・理由・列挙はしない」「短さを最優先」と縛ると、
**相手の発話を圧縮して「〜だね」を付けるのが最も安全な出力になる**。
[例] も「今日はいい天気だね」→「きもち、いいね」と部分的にオウム返しを教えていた。

ルール7（相手が言った言葉をそのまま返さない／相手の発話に無い言葉を必ず入れる／
進め方は 自分の話・質問・別の見方 のどれか）を追加し、[例] を話が進む形に差し替えた。

#### 測定（実対話ログの発話7つを、履歴を積み上げながら流した）

指標は**新語率**＝応答の文字二重字のうち、直前のユーザー発話に無いものの割合。
完全なオウム返しなら0に近づく。

| | 新語率 中央 | 新語率 最小 | モーラ中央 | 生成 p50 |
|---|---|---|---|---|
| 旧 | 60% | 20% | 12 | 0.76秒 |
| **新** | **100%** | **80%** | **9** | 0.71秒 |

**制約を足したのに応答は短くなり、生成も遅くならなかった。** オウム返しは短さの手段ではなく、
むしろ冗長だったということ。聞き返し（ルール8）も3回試して段階的な言い直しが保たれている。

**この2軸は独立ではない。** オウム返しの間は聞き手が自分の発話から内容を復元できるため、
差し替え率0.5で成立して見えていた可能性がある。差し替え率はこの変更のあとで聞き直して決めること。
ログには毎ターン `swap_ratio` を残している。

→ 聞き直した結果、**予想と逆に振れた**（下の「差し替え率を0.67に上げた」）。

### 立ち上がりを0.4秒にした根拠（2026-08-19）

**実際に対話したら食い気味に聞こえた。** 認識が確定した瞬間に鳴らしていたのが原因。
人は相手が黙った瞬間に言い淀むのではなく、少し置いてから言い淀む。

`VoiceVoxFillerPlayer(initial_delay_sec=...)` を足した（既定0.4秒、`--filler-delay` で変更可）。
**VAD の `stability_duration`（0.4秒）は触っていない。** そちらを動かすと認識の挙動そのものが
変わり、フィラー無し条件にも影響が出て実験の比較が壊れる。フィラー側だけで足りる。

使える上限は「最短の待機区間 − フィラー最長」で決まる。

| | 実測 |
|---|---|
| Gemini（thinking=0）の最短 | 0.46秒 |
| 合成の最短（最も短い応答） | 0.84秒 |
| **最短の待機区間** | **1.30秒** |
| フィラーの長さ | えーと 0.811 / あのー 0.768 / うーん 0.789 / えーっと 0.917 / あー 0.683 秒 |
| **フィラー最長** | **0.92秒** |
| **遅延に使える上限** | **約0.38秒** |

これを超えると、フィラーを鳴らし終わるまで応答が待たされる。
**この上限は後に撤回した**（下の「応答の文頭から感動詞を外した」参照）。
待たせてよいと決めたので、上限は守るべき制約ではなく、遅れの大きさを見積もる目安として使う。

遅延中に停止されたら一度も鳴らさずに終わる。応答が異常に速く返ったときにフィラーだけ
後から鳴る事故を防ぐため。0 を渡せば従来どおり即座に鳴るので、比較条件も作れる。

### 間隔を2.0秒に決めた根拠（2026-08-17）

```
埋める区間 = LLM 0.9秒 + 合成 1.1秒 = 約2.0秒（遅いターンで3.5秒程度）
1つ目「えーと」= 0.81秒
```

| 間隔 | 2つ目が出る時刻 | 挙動 |
|---|---|---|
| 1.0秒 | 1.81秒 | ほぼ毎回2つ出る。うるさい |
| 1.5秒（当初） | 2.31秒 | **応答ができるのとほぼ同時。最もかち合う** |
| **2.0秒（採用）** | **2.81秒** | **普通のターンでは出ない。遅いときだけ出る** |
| 3.0秒 | 3.81秒 | ほぼ出ない。実質1つだけ |

当初の1.5秒は、`thinking` を切る前（無音4.8秒）の想定で決めた値で、高速化後の実測に合っていなかった。実機確認で3回中1回、**2つ目を鳴らし始めた直後に応答ができて0.9秒待たされる**事象が出た。原因は `winsound.PlaySound` が同期再生で中断できないこと（意図的にそうしてある。語の途中で切ると不自然なため）。**フィラーあり条件だけ応答が遅くなると実験の交絡になる**ので、間隔で回避する。

### 動作確認

ASR をモックにして生成・合成・再生を本物で通した（`--simulate` でも確認）。フィラーが応答に重ならないこと、停止の遅延が記録されることを確認済み。**マイク入力を含む通し動作は未確認**（項目「対話全体の通し動作」参照）。

### 検討したが採らなかったこと

フィラーを待機フェーズにマッピングする設計（Phase A/B/C に「んー」「えーと」「あのー」を割り当てる）を検討したが、**今は採らない**。既定の3パターンを回すだけで足りる。再検討する場合の材料：

- **前川喜久雄（2012）のCSJ分析では、設計案と逆の分布が出ている。** 「あの・あのー」はポーズの**先頭**（relPosit≈0.0）、「ん」はポーズの**末尾**（relPosit≈1.0）に集中する。早期に「んー」・後期に「あのー」という割り当ては、このデータとは逆
- ただし前川・太田ともに**モノローグ（学会/模擬講演）**の分析で、対話のターン交代ではない。「んー」を相槌的に早期に置くという理屈は対話固有のもので、単純には比較できない
- **時間の絶対値（1.2秒などの閾値）はどちらの論文にも無い。** 前川の relPosit は相対位置（比率）
- 「そのー＝言い訳的ニュアンス」もこの2本には記述が無い
- えーと／あのー の機能的区別は定延・田窪(1995)「談話における心的操作モニター機構」言語研究108, pp.74-93。**未入手・未確認**
- **使えそうな知見**：フィラーのピッチは語彙的に決まっておらず前後の文脈の補間で決まり、**ポーズの後半に出るものほど高い**（前川）。単調さが気になったとき、2つ目の `pitchScale` を上げる根拠に使える

---

---

## その他の修正

- マイク入力デバイスを `.env` の `STT_INPUT_DEVICE` / `--input-device` で指定できるようにした。
  この PC の OS 既定入力が「ステレオ ミキサー」（ループバック）で、そのままでは声が入らないため
- `VoiceVoxTTS` の既定URLを `localhost` → `127.0.0.1` に変更。IPv6 フォールバックで
  TCP接続のたびに約2秒待たされていた
- `VoiceVoxTTS` に `raise_for_status()` を追加。失敗応答をそのまま wav に書いていた
- テストの `is_voicevox_running()` が `localhost` + `timeout=0.5` だったため、VOICEVOX が
  起動していても依存テストが必ず skip されていた。127.0.0.1 に修正して実際に走るようになった

